spring:
    application:
        name: The_Anarchy_Helper
    datasource:
        url: jdbc:mysql://localhost:3306/the_anarchy_helper
        username: root
        password: admin
    jpa:
        generate-ddl: false
        hibernate:
            ddl-auto: validate
    flyway:
        enabled: true
        locations: classpath:db/migration